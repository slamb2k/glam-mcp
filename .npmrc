# NPM Configuration for slambed-mcp
# This file configures npm behavior for the project

# Save exact versions
save-exact=true

# Use package-lock.json
package-lock=true

# Audit level for security vulnerabilities
audit-level=moderate

# Registry URL (default npm registry)
registry=https://registry.npmjs.org/

# Optional: Configure for scoped packages
# @yourscope:registry=https://npm.pkg.github.com

# Don't include optional dependencies by default
omit=optional

# Progress bar during installation
progress=true